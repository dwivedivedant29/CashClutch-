name: cashclutch
description: "US Rewarded Play to Earn App"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^3.12.0
  firebase_auth: ^5.5.0
  cloud_firestore: ^5.6.5
  google_sign_in: ^6.2.2
  url_launcher: ^6.3.0

flutter:
  uses-material-design: true
